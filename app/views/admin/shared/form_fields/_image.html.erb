<div class="control-group">
  <%= f.label column.to_sym, :class => 'control-label' %>
  <div class="controls">
    <% if !f.object.send(column).blank? %>
      <%= image_tag f.object.image_url(:thumb) %><br><br>
    <% end %>
    <%= f.file_field column.to_sym %>
  </div>
</div>